target
.snfoundry_cache/
snfoundry_trace/
coverage/
profile/
.env

# Claude agent (internal tooling, not for delivery)
.claude/

# Git worktrees
.worktrees/

# Lock files (client generates their own)
Cargo.lock
Scarb.lock
